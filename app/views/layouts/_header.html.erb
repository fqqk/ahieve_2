<header class="header">
<nav class="d-flex justify-content-between bg-dark">
  <div class="d-flex">
    <p><%= link_to "Home", root_path%></p>
    <p><%= link_to "blogs", blogs_path %></p>
    <p><%= link_to "contact", new_contact_path %></p>
  </div>
  <div class="d-flex">
     <% if user_signed_in? %>
     <p><%= link_to "logout", destroy_user_session_path, method: :delete %></p>
     <p><%= link_to "edit-prof", edit_user_registration_path, method: :delete %></p>
     <% end %>
  </div>
</nav>
</header>
